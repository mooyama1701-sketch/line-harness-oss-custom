# Project Instructions

- ゴールから外れる提案をしないでください。
- ゴールに進む提案を必ずしてください。
- 回答には必ず「次のタスクはこれ」「今の進捗を全体像から整理するとこれ」を含めてください。
- 私が大学生だと思って、言語化してください。

---

# カスタマイズ版プロジェクト固有ルール

## 1. このプロジェクトについて

このリポジトリは、OSSの「LINE Harness」をベースにした改造版を開発し、第三者へ提供できる状態にするためのプロジェクトです。

最終的には、第三者が以下のようなnpxコマンドを実行し、自分自身のCloudflareアカウント上に改造版LINE Harnessを新規構築できるようにします。

```bash
npx @airestart/create-line-harness
```

初期リリースでは、新規環境の構築だけを対象とします。

既存環境の自動更新、既存D1からのデータ移行、ロールバック、自動バックアップは、初期リリースの対象外です。

## 2. プロジェクト資料

プロジェクト全体の計画は、以下のファイルを正とします。

```text
docs/プロジェクト計画書.md
```

通常の小さな調査・修正では、毎回このファイルを全文読み直す必要はありません。

ただし、以下に該当する作業では、作業前に関連箇所を確認してください。

* プロジェクト全体の方針を変更する場合
* 初期リリースの対象範囲を変更する場合
* アーキテクチャやディレクトリ構成を変更する場合
* Cloudflareへデプロイする場合
* D1マイグレーションを実行する場合
* npmパッケージを公開する場合
* GitHub Releaseを作成する場合
* セキュリティや秘密情報に関係する場合
* 既存本番環境に関係する場合
* 計画書と実装内容が食い違う可能性がある場合

計画書と個別の作業指示が矛盾する場合は、勝手に判断せず、矛盾点を報告してください。

## 3. 初期リリースの対象

初期リリースで対象とするものは以下です。

* 改造版LINE Harness本体
* 改造版専用インストーラー
* 新規Cloudflare Workerの作成
* 新規Cloudflare D1の作成
* 新規Cloudflare R2の作成
* 新規Cloudflare Pagesの作成
* Worker Secretsの登録
* D1マイグレーション
* LINE公式アカウント情報の登録
* 初期管理者の作成
* セットアップ完了結果の表示
* 第三者向けインストールマニュアル
* MIT Licenseと元OSSの著作権表示

初期リリースでは以下を実装しません。

* 既存環境の自動更新
* 既存D1からのデータ移行
* 自動ロールバック
* 自動バックアップ
* 有料ライセンス認証
* テレメトリー
* SaaS化
* 大山が管理する外部サーバーへの秘密情報送信

## 4. 基本作業方針

作業では、以下の順番を基本としてください。

1. 現在の状態を確認する
2. 関連するコードと資料を読む
3. 影響範囲を整理する
4. 必要に応じて実装計画を提示する
5. 指示された範囲だけ変更する
6. テスト、型チェック、Lintなどを実行する
7. Git差分を確認する
8. 実施内容、確認結果、残課題を報告する

推測だけで変更せず、可能な限り実際のコード、設定、コマンド結果を確認してください。

不明な点は、「確認済み」「推測」「未確認」を区別して報告してください。

## 5. 変更範囲

依頼された範囲を超えて、無関係なコードを変更しないでください。

以下は原則として避けてください。

* 大規模な一括リファクタリング
* 依頼されていないフォーマット変更
* 無関係な依存パッケージの更新
* ロックファイル全体の不要な変更
* 既存仕様の無断変更
* 将来用という理由だけの過剰実装
* 初期リリース対象外機能の先行実装

改善案がある場合は、今回の変更へ混ぜず、提案として別に報告してください。

## 6. 本番環境の保護

現在稼働中のLINE Harness本番環境と、この改造版プロジェクトは分離してください。

現在把握している本番リソースは以下です。

```text
Worker：
line-harness-production

D1：
line-harness

Pages：
line-harness-production-admin
```

以下を、明示的な許可なしに操作してはいけません。

* 本番Cloudflare Worker
* 本番D1
* 本番R2
* 本番Pages
* 本番のWorker Secrets
* 本番LINE公式アカウント
* 本番Webhook URL
* 本番LIFF設定
* 本番用ローカルリポジトリ

本番環境へ影響する可能性がある場合は、実行せず、影響内容を先に報告してください。

検証では、必ずdev用の別名リソースを使用してください。

推奨する仮名称は以下です。

```text
Worker：
line-harness-custom-dev

D1：
line-harness-custom-dev

R2：
line-harness-custom-dev-images

Pages：
line-harness-custom-dev-admin
```

## 7. 外部サービス操作

以下の操作は、ユーザーからその作業について明示的な指示がある場合だけ実行してください。

* GitHubへのログイン
* GitHubリポジトリの作成
* GitHub上でのFork
* git push
* Pull Requestの作成
* npm publish
* GitHub Releaseの作成
* Cloudflareへのログイン
* Wranglerによるデプロイ
* D1マイグレーション
* D1データの変更
* Worker Secretsの登録・変更
* R2やPagesの作成・変更・削除
* LINE APIへの接続
* LINE Developers設定の変更
* Webhook URLの変更
* LIFF設定の変更

「調査してください」という依頼では、原則としてread-only操作だけを行ってください。

## 8. Gitの運用ルール

ファイルを変更する前後に、以下を確認してください。

```bash
git status --short --branch
```

作業後は、以下を報告してください。

* 変更したファイル
* 新規作成したファイル
* 削除したファイル
* 実行したテスト
* テスト結果
* 未解決事項

以下は、明示的な指示なしに実行しないでください。

```text
git commit
git push
git pull
git merge
git rebase
git reset --hard
git clean
git tag
```

コミットを依頼された場合は、一つのコミットに複数の無関係な変更を混ぜないでください。

## 9. 秘密情報の取り扱い

以下の秘密情報を、回答、ログ、コード、Git、npmパッケージへ平文で出力しないでください。

* Cloudflare API Token
* Cloudflare認証情報
* LINEチャネルアクセストークン
* LINEチャネルシークレット
* LIFF関連の秘密情報
* GitHub Token
* npm Token
* APIキー
* パスワード
* Cookie
* セッション情報

秘密情報を確認する必要がある場合も、値そのものを表示せず、設定の有無だけを報告してください。

秘密情報を以下へ保存しないでください。

* Git管理対象ファイル
* README
* AGENTS.md
* プロジェクト計画書
* テスト結果
* npm公開対象ファイル

## 10. インストーラーの安全原則

改造版インストーラーは、第三者自身のPCから、第三者自身のCloudflareへ直接接続する設計とします。

第三者の秘密情報を、大山が管理するサーバーやAPIへ送信してはいけません。

インストーラーでは、以下を安全上の必須事項とします。

* 既存Worker名の重複確認
* 既存D1名の重複確認
* 既存R2名の重複確認
* 既存Pages名の重複確認
* 既存リソースの無断上書き禁止
* 実行前の構築内容確認
* 秘密情報のマスク
* 失敗時の原因表示
* 再実行方法の案内
* 作成済みリソースの明確な報告

初期版では、公式版または既存環境を自動更新する処理を有効にしないでください。

## 11. 依存パッケージ

新しい本番依存パッケージを追加する前に、以下を確認してください。

* 本当に必要か
* 標準機能または既存依存で代替できないか
* メンテナンス状況
* ライセンス
* セキュリティ上の問題
* npmパッケージサイズへの影響

新しい本番依存パッケージは、明示的な許可なしに追加しないでください。

開発用依存パッケージについても、追加理由を報告してください。

## 12. ライセンス

このプロジェクトは、元のLINE HarnessのMIT Licenseと著作権表示を維持します。

以下を無断で削除・変更しないでください。

* LICENSE
* 元開発者の著作権表示
* 元OSSに関するクレジット
* 再配布に必要なライセンス表記

ライセンス上の判断が不明な場合は、断定せず、確認事項として報告してください。

## 13. テストと検証

コードを変更した場合は、変更範囲に応じたテストを実行してください。

利用可能になった後は、最低限以下を検討してください。

```text
・Lint
・型チェック
・単体テスト
・ビルド
・CLIのヘルプ表示
・dry-run
・新規Cloudflareリソースの構築テスト
```

実際のCloudflareやLINEへ接続するテストは、明示的な許可がある場合だけ行ってください。

テストを実行できなかった場合は、その理由を報告してください。

## 14. 作業報告

作業完了時は、原則として以下の形式で日本語で報告してください。

```markdown
# 作業結果

## 実施内容

- ...

## 変更したファイル

- ...

## 実行した確認・テスト

- ...

## 確認結果

- ...

## 本番環境への影響

- なし／あり
- 理由：

## 残っている課題

- ...

## 次に推奨する作業

- ...
```

調査だけの場合は、変更したファイルがないことを明記してください。

## 15. コミュニケーション

報告は日本語で行ってください。

専門用語を使用する場合は、必要に応じて初心者にも分かる説明を添えてください。

コマンドを提示する場合は、以下を区別してください。

* 実際に実行したコマンド
* 実行を提案するだけのコマンド
* ユーザーが手動で実行するコマンド

危険な操作や元に戻しにくい操作は、実行前にその理由と影響を説明してください。

## 16. プロジェクト管理ドキュメント

プロジェクトの現在地と重要事項は、以下のファイルで管理します。

* `docs/PROJECT_STATUS.md`
* `docs/DECISIONS.md`
* `docs/INSTALLER_ARCHITECTURE.md`
* `docs/RELEASE_CHECKLIST.md`

作業開始時は、最低限 `docs/PROJECT_STATUS.md` を確認してください。

作業によって以下を更新してください。

* 作業が進んだ場合：`PROJECT_STATUS.md`
* 重要な方針を決定した場合：`DECISIONS.md`
* インストーラー設計を変更した場合：`INSTALLER_ARCHITECTURE.md`
* リリース条件を確認した場合：`RELEASE_CHECKLIST.md`

完了していない作業や、確認できていない事項を完了済みとして記録しないでください。

過去の決定事項は削除せず、変更された場合は「置換済み」「廃止」などの状態を記録してください。
